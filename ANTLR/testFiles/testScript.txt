input:

read filepath.csv as inFile;
read folder/fsecondFile.csv as alternate (noRowHeader);

end input;


actions:

inFile subset quiz = [col quiz1, col quiz2 to col quiz5];
inFile subset groupA = [row tommy, row billy, row joe];
inFile subset bestQuizGrades = [tommy.quiz4, billy.quiz2];

function add2ExtraCredit = value + 2;
function convertToPercent = value / total.value;

scheme classWeights = {
    quiz: value * 0.2,
    test: value * 0.4,
    project: value *0.4
};

cell max = tommy.quiz2;
if (entry > max)
	cell max = entry;
end if;

for item in [quiz1, quiz2 to quiz5]
	function total = total + item;
end for;

end actions;


output:

use classWeights on inFile;
use add2ExtraCredit on groupA.quiz2;
write inFile out.csv;

end output;